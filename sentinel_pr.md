## Sentinel Target
Coverage gap in math logic for `rsu_budget_plan` calculations and weak visual representation test bounds in `logos-cli` budget tables.

## Strategy
1. Covered math bounds for `.max(0)`, division operations, and multiplication rounding.
2. Verified visual table generation in CLI with strict header bound expectations.
3. Updated Sentry Journal.
