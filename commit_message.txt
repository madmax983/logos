🤖 Sentinel: Strengthen HomeSnapshot test assertions

### 🔭 Mutants Found
4 surviving mutants in `crates/logos-tui/src/app.rs` related to `HomeSnapshot` accessors returning defaults (e.g. `expense_account_prefix` returning `""`, `checking_balance_cents` returning `0`).

### 🎯 Tests Added
Strengthened `home_view_renders_month_cashflow_and_budget_summary` in `tests/home_screen.rs`. Updated the mock `HomeSnapshot` to use completely unique strings and integer values for every field so that cross-contamination is impossible, and assertions explicitly check for these unique rendered values.

### ⚠️ Suspected Bugs
None.

### 📊 Kill Rate
100% of the targeted `HomeSnapshot` mutant accessors are now killed.
