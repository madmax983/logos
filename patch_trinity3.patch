--- crates/logos-core/src/experimental/trinity_simulator.rs
+++ crates/logos-core/src/experimental/trinity_simulator.rs
@@ -311,7 +311,7 @@
         // We will test exactly with seed 12345 since it's deterministic.
         let sim_volatile = TrinitySimulator::new(100, 10, 0.05, 0.20, 0.03, 12345);

-        let result = sim_volatile.run(1, 10);
+        let _result = sim_volatile.run(1, 10);

         // With seed 12345, the random normal draws will cause exactly some paths to fail
         // and some to succeed. We run 10 paths.
@@ -353,7 +353,7 @@
             0.0, 1.0, // high volatility
             0.0, 42,
         );
-        let result = sim.run(1, 3);
+        let _result = sim.run(1, 3);
         // Seed 42 for 3 paths exactly.
         // Let's assert the exact number it results in.
