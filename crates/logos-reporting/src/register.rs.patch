<<<<<<< SEARCH
    #[test]
    fn project_register_balance_saturates_on_overflow() {
        let entries = [RegisterEntry::new(1)];
        let final_balance = project_register_balance(i64::MAX, &entries);
        assert_eq!(final_balance, i64::MAX);

        let entries_under = [RegisterEntry::new(-1)];
        let final_balance_under = project_register_balance(i64::MIN, &entries_under);
        assert_eq!(final_balance_under, i64::MIN);
    }
=======
    #[test]
    fn project_register_balance_saturates_on_overflow() {
        let entries = [RegisterEntry::new(1)];
        let final_balance = project_register_balance(i64::MAX, &entries);
        assert_eq!(final_balance, i64::MAX);
    }

    #[test]
    fn project_register_balance_saturates_on_underflow() {
        let entries_under = [RegisterEntry::new(-1)];
        let final_balance_under = project_register_balance(i64::MIN, &entries_under);
        assert_eq!(final_balance_under, i64::MIN);
    }

    #[test]
    fn project_register_balance_iter_saturates_on_overflow() {
        let entries = [RegisterEntry::new(1)];
        let final_balance = project_register_balance_iter(i64::MAX, entries);
        assert_eq!(final_balance, i64::MAX);
    }

    #[test]
    fn project_register_balance_iter_saturates_on_underflow() {
        let entries_under = [RegisterEntry::new(-1)];
        let final_balance_under = project_register_balance_iter(i64::MIN, entries_under);
        assert_eq!(final_balance_under, i64::MIN);
    }
>>>>>>> REPLACE
