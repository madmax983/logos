use crate::args::CliError;

/// Handles `ledger budget set`.
///
/// # Errors
///
/// This placeholder never errors.
pub fn set() -> Result<(), CliError> {
    println!("budget.set placeholder");
    Ok(())
}
