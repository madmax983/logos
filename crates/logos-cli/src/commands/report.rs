use crate::args::CliError;

/// Handles `ledger report month`.
///
/// # Errors
///
/// This placeholder never errors.
pub fn month() -> Result<(), CliError> {
    println!("report.month placeholder");
    Ok(())
}
