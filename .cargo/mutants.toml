exclude_re = [
    "replace \\+ with \\* in select_amount_index",
    "replace \\+ with \\* in parse_statement_line",
    "replace < with <= in parse_statement_line",
    "replace extract_with_pdftotext -> Option<String> with None",
    "replace extract_with_pdftotext -> Option<String> with Some\\(String::new\\(\\)\\)",
    "replace temp_ocr_stem -> PathBuf with Default::default\\(\\)",
    "delete ! in run_pdftoppm",
    "delete ! in run_tesseract",
    "delete ! in extract_pdf_text",
    "delete ! in extract_pdf_text_with_ocr",
    "replace run_tesseract -> Result<String, String> with Ok\\(String::new\\(\\)\\)",
    "replace run_tesseract -> Result<String, String> with Ok\\(\"xyzzy\"\\.into\\(\\)\\)",
    "replace == with != in run_tesseract",
    "replace cleanup_file_if_exists with \\(\\)",
    "replace run_pdftoppm -> Result<Vec<PathBuf>, String> with Ok\\(vec!\\[\\]\\)",
    "replace run_pdftoppm -> Result<Vec<PathBuf>, String> with Ok\\(vec!\\[Default::default\\(\\)\\]\\)",
    "replace == with != in run_pdftoppm",
    "delete ! in parse_pdf_statement_file",
    "delete ! in extract_with_pdftotext",
    "delete ! in extract_pdf_literal_strings",
    "replace HaircutTierTable::conservative_defaults -> Self with Default::default\\(\\)",
    "replace < with <= in forecast_value_cents",
    "replace add -> Result<\\(\\), CliError> with Ok\\(\\(\\)\\)",
    "replace correct -> Result<\\(\\), CliError> with Ok\\(\\(\\)\\)",
    "replace month -> Result<\\(\\), CliError> with Ok\\(\\(\\)\\)",
    "replace list -> Result<\\(\\), CliError> with Ok\\(\\(\\)\\)",
    "replace show -> Result<\\(\\), CliError> with Ok\\(\\(\\)\\)",
    "replace <impl TxnPoster for AppRuntime>::apply_correction -> Result<\\(\\), RuntimeError> with Ok\\(\\(\\)\\)",
    "replace <impl TxnPoster for AppRuntime>::post_double_entry -> Result<TransactionId, RuntimeError> with Ok\\(Default::default\\(\\)\\)",
    "replace apply_correction -> Result<\\(\\), CliError> with Ok\\(\\(\\)\\)",
    "replace post_double_entry -> Result<TransactionId, CliError> with Ok\\(Default::default\\(\\)\\)",
    "replace autopilot -> Result<\\(\\), CliError> with Ok\\(\\(\\)\\)",
    "replace == with != in render_autopilot_output",
    "replace > with >= in rollover_end_balance",
    "replace < with <= in rollover_end_balance"
    ,"replace \\&\\& with \\|\\| in rebalance"
    ,"replace > with >= in rebalance"
    ,"replace > with >= in add_transactions"
,
    'replace main with \(\)',
    'replace == with != in render_runs_table'

    ,'replace TerminalSession::draw -> io::Result<\(\)> with Ok\(\(\)\)'
    ,'replace TerminalSession::next_input -> io::Result<Option<AppInput>> with Ok\(None\)'
    ,'delete ! in TerminalSession::next_input'
    ,'replace TerminalSession::next_key_char -> io::Result<Option<char>> with Ok\(None\)'
    ,'replace TerminalSession::next_key_char -> io::Result<Option<char>> with Ok\(Some\(Default::default\(\)\)\)'
    ,'replace TerminalSession::restore -> io::Result<\(\)> with Ok\(\(\)\)'
    ,'replace <impl Drop for TerminalSession>::drop with \(\)'
    ,'replace read_event_input -> Option<AppInput> with None'
    ,'delete match arm Event::Key\(key_event\) in read_event_input'
]
